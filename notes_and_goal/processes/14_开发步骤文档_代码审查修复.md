# 开发步骤文档 - 代码审查修复

> 基于 [11_代码审查报告_2026-06-07.md](./11_代码审查报告_2026-06-07.md) 的修复计划

## 用户决策记录

| 问题 | 决策 |
|------|------|
| C2 - 同步ID方案 | 添加syncId字段(UUID)，迁移现有数据 |
| C1 - 提醒调度位置 | 使用ref.listen监听isarProvider |
| M3 - 导入行为 | 重置为新任务（保持现状，不修改） |
| ME5 - 提醒无截止日期 | 不做处理（保持现状，不修改） |

---

## 步骤一：Task模型添加syncId字段（C2基础）

**文件**: `lib/models/task.dart`

**修改内容**:
- 添加 `@Index(unique: true) String? syncId;` 字段
- 在构造函数中生成默认UUID
- 更新 `copyWith` 方法支持syncId

**依赖**: 无

---

## 步骤二：CatodoIO导入/导出支持syncId（C2配套）

**文件**: `lib/services/catodo_io_service.dart`

**修改内容**:
- `_taskToJson` 添加 `'syncId': t.syncId`
- `_jsonToTask` 添加 `syncId: t['syncId']`

**依赖**: 步骤一

---

## 步骤三：WebDAV同步使用syncId匹配（C2核心）

**文件**: `lib/services/webdav_service.dart`

**修改内容**:
- `_tasksToJson` 添加syncId字段
- `_jsonToTasks` 解析syncId字段
- `sync` 方法：将 `remoteMap = {for (var t in remoteTasks) t.id: t}` 改为 `{for (var t in remoteTasks) t.syncId: t}`，本地同理
- `_resolveConflict` 计数逻辑修复（ME3一并修复）

**依赖**: 步骤一、步骤二

---

## 步骤四：修复sync方法冲突计数逻辑（ME3）

**文件**: `lib/services/webdav_service.dart`

**修改内容**:
- 在调用 `_resolveConflict` 前保存比较结果，用布尔变量标记本地胜出/远程胜出
- 修复 `autoMerge` 模式下计数始终为uploadedCount的问题

**依赖**: 步骤三（同文件修改）

---

## 步骤五：修复task_list_screen同步重复插入（C3）

**文件**: `lib/ui/screens/task_list_screen.dart`

**修改内容**:
- 删除 `_performSync` 中二次 `downloadTasks()` + `insertTask()` 的逻辑
- 改为使用 `sync` 返回的 `mergedTasks` 通过 `updateTask` 写回本地
- 同步时传入SyncMode（M2一并修复）

**依赖**: 步骤三

---

## 步骤六：task_list_screen同步传入SyncMode（M2）

**文件**: `lib/ui/screens/task_list_screen.dart`

**修改内容**:
- `_performSync` 中从 `ref.read(syncModeProvider)` 获取模式
- 传入 `service.sync(localTasks, mode: syncMode)`

**依赖**: 步骤五（同文件修改）

---

## 步骤七：main.dart - 提醒调度改为ref.listen（C1）

**文件**: `lib/main.dart`

**修改内容**:
- 删除 `Consumer` builder 中的 `_rescheduleReminders(isar)` 调用
- 添加 `ref.listen` 监听 `isarProvider`，数据就绪时执行一次调度
- `_rescheduleReminders` 参数类型从 `dynamic` 改为 `Isar`（ME1一并修复）

**依赖**: 无

---

## 步骤八：task_item.dart ref.watch改为ref.read（M1）

**文件**: `lib/ui/components/task_item.dart`

**修改内容**:
- `_toggleComplete` 方法中 `ref.watch(isarProvider.future)` 改为 `ref.read(isarProvider.future)`

**依赖**: 无

---

## 步骤九：ics_service.dart 转义顺序修复（M4）

**文件**: `lib/services/ics_service.dart`

**修改内容**:
- `_unescapeIcs` 方法中调整 replaceAll 顺序：先 `\\\\` → `\\`，再 `\\,` → `,`，最后 `\\n` → `\n`

**依赖**: 无

---

## 步骤十：day_view_screen.dart 提取辅助方法（ME2）

**文件**: `lib/ui/screens/day_view_screen.dart`

**修改内容**:
- 将 `_formatDateHeader` 和 `_modeLabel` 从 `build()` 内部提取为类的私有方法

**依赖**: 无

---

## 步骤十一：notification_service_io.dart 通知ID生成（ME4）

**文件**: `lib/services/notification_service_io.dart`

**修改内容**:
- 使用 `task.syncId.hashCode + i` 替代 `task.id * 10 + i` 生成通知ID（需先完成步骤一）
- 或使用 `Object.hash(task.syncId, i)` 生成唯一ID

**依赖**: 步骤一

---

## 步骤十二：filter.dart 添加==和hashCode（L4）

**文件**: `lib/models/filter.dart`

**修改内容**:
- 添加 `==` 和 `hashCode` 重写，基于 `selectedGroup`、`selectedPriority`、`selectedTag` 字段

**依赖**: 无

---

## 步骤十三：chat_screen.dart 添加错误处理（L1）

**文件**: `lib/ui/screens/chat_screen.dart`

**修改内容**:
- `_initAIService` 方法添加 try-catch 包裹

**依赖**: 无

---

## 步骤十四：print替换为debugPrint（L5）

**文件**: `lib/services/webdav_service.dart`, `lib/services/ai_service.dart`, `lib/main.dart`

**修改内容**:
- 所有 `print(` 替换为 `debugPrint(`

**依赖**: 无

---

## 执行顺序

```
步骤一（Task syncId）
  → 步骤二（CatodoIO）  ─┐
  → 步骤三（WebDAV同步）  ├─ 步骤十一（通知ID）
  → 步骤四（计数逻辑）    │
  → 步骤五（C3重复插入）  │
  → 步骤六（M2 SyncMode） ┘
步骤七（C1提醒调度）
步骤八（M1 ref.watch）
步骤九（M4 ICS转义）
步骤十（ME2 提取方法）
步骤十二（L4 filter）
步骤十三（L1 chat错误处理）
步骤十四（L5 print替换）
```

**变更说明**：M3（导入行为）和 ME5（提醒无截止日期）根据用户决策保持现状，不修改。L2、L3同样保持现状。